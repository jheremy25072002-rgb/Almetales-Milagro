rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /arqueos/{recicladoraId} {
      allow read, write: if request.auth != null;
    }

    match /compras_diarias/{fecha} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
